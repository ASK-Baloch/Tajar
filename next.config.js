/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "localhost",
            "Tajar-production.up.railway.app/"
        ]
    }
}

module.exports = nextConfig
