/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "localhost",
            "simple-project-production.up.railway.app/"
        ]
    }
}

module.exports = nextConfig
